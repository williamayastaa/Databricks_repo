# The main pipeline for Dbasocciate_final_project

resources:
  pipelines:
    Dbasocciate_final_project_etl:
      name: Dbasocciate_final_project_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/Dbasocciate_final_project_etl"

      libraries:
        - glob:
            include: ../src/Dbasocciate_final_project_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
